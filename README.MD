Features:

Set workspace
Set run command
Show project tree
List directory
Read file
Read multiple files
Search text across the workspace
Create file
Replace text in a file
Replace an entire function using Tree-sitter
Run any terminal command
Run the configured project
Wait for a process to finish and get stdout, stderr and exit code
View live terminal output of a running process
View the last N lines of terminal output
Persist workspace and run command in config.json across server restarts
Restrict all file operations to the current workspace
Read compiler/runtime errors directly from program output
Use ChatGPT to edit code through MCP tools instead of manually copying files